services:
  db:
    image: postgres:14.17-bookworm
    volumes:
      - pgdata:/var/lib/postgresql/data 
    environment:
      POSTGRES_PASSWORD: optmize
      POSTGRES_DB: optmize

    ports:
      - 6968:5432
    

  adminer:
    image: adminer
    ports:
    - 8080:8080
    
volumes:
  pgdata: